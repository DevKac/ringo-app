{
  "name": "ringo-app",
  "version": "1.0.0",
  "description": "to be done",
  "main": "server.js",
  "scripts": {
    "install-packages": "npm install && cd ./public/ringo-app && npm install",
    "start-server": "cd ./public/ringo-app && npm run build && cd ../../ && node server.js",
    "start-app": "cd ./public/ringo-app && npm start"
  },
  "author": "DevKac",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
